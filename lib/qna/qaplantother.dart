import 'package:flutter/material.dart';

class plantotheroption extends StatelessWidget {
  const plantotheroption({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
      child: Text("We don't have your searched specie in our database."),
    ),
    );
  }
}
