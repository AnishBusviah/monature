import 'package:flutter/material.dart';

class animalother extends StatelessWidget {
  const animalother({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("We don't have your searched animal in our database."),
    );
  }
}
